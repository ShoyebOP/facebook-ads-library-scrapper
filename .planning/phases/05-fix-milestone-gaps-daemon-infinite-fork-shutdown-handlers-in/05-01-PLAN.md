---
phase: 05-fix-milestone-gaps-daemon-infinite-fork-shutdown-handlers-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/daemon.ts
  - src/index.ts
  - src/cli.ts
  - src/types.ts
autonomous: true
requirements:
  - DAEMON-01
  - SCRAPE-10
must_haves:
  truths:
    - Child process no longer forks infinitely when --daemon flag is passed
    - CLI exits with clear error when --proxy is provided without a URL value
    - ScraperOptions interface includes optional incrementalSaver property
  artifacts:
    - src/daemon.ts (SCRAPER_DAEMON_CHILD env var set on fork)
    - src/index.ts (env var check before fork decision)
    - src/cli.ts (proxy value validation added)
    - src/types.ts (incrementalSaver property on ScraperOptions)
  key_links:
    - daemon.ts fork options → SCRAPER_DAEMON_CHILD=1 propagates to child
    - index.ts main() → checks process.env.SCRAPER_DAEMON_CHILD before fork block
---

<objective>
Fix the critical daemon infinite fork bug, add missing CLI proxy validation, and prepare the type system for incremental saver integration.

Purpose: The daemon infinite fork is a showstopper — child process re-enters the --daemon block and forks again endlessly. The CLI proxy validation gap causes cryptic browser errors. The type prep enables Plan 2's incremental saver wiring.

Output: daemon.ts sets SCRAPER_DAEMON_CHILD=1 on fork, index.ts checks it before forking, cli.ts validates proxy values, types.ts has incrementalSaver property.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-milestone-gaps-daemon-infinite-fork-shutdown-handlers-in/05-CONTEXT.md
@.planning/phases/05-fix-milestone-gaps-daemon-infinite-fork-shutdown-handlers-in/05-RESEARCH.md
@.planning/phases/05-fix-milestone-gaps-daemon-infinite-fork-shutdown-handlers-in/05-PATTERNS.md
@src/daemon.ts
@src/index.ts
@src/cli.ts
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix daemon infinite fork + CLI proxy validation</name>
  <files>src/daemon.ts, src/index.ts, src/cli.ts</files>
  <read_first>
    src/daemon.ts (lines 57-92: startDaemon function, fork call at line 78)
    src/index.ts (lines 36-44: daemon fork block in main)
    src/cli.ts (lines 67-88: existing validation block)
  </read_first>
  <behavior>
    - Test: startDaemon sets SCRAPER_DAEMON_CHILD=1 in fork env options (verify env key exists in fork call)
    - Test: main() skips fork when SCRAPER_DAEMON_CHILD=1 is set (verify env var check added)
    - Test: main() forks normally when SCRAPER_DAEMON_CHILD is not set (existing behavior preserved)
    - Test: cli.ts exits with error when --proxy is provided as empty string
    - Test: cli.ts allows --proxy with a valid URL value
  </behavior>
  <action>
**Part A — Daemon fork prevention (per D-01, D-02, D-03, D-04):**

In `src/daemon.ts`, modify the `startDaemon()` function (line 78) to set the SCRAPER_DAEMON_CHILD environment variable in the fork options. Add `env: { ...process.env, SCRAPER_DAEMON_CHILD: '1' }` to the fork options object. This ensures the child process inherits all parent env vars plus the marker.

In `src/index.ts`, modify the daemon fork block (line 37) to check `process.env.SCRAPER_DAEMON_CHILD !== '1'` before entering the fork path. Change `if (argv.daemon)` to `if (argv.daemon && process.env.SCRAPER_DAEMON_CHILD !== '1')`. When the env var IS set, the code falls through to the normal scraper execution (the child runs the scraper directly).

**Part B — CLI proxy validation (per D-14):**

In `src/cli.ts`, add a proxy value check after the existing numeric validation block (after line 88). Check: if `argv.proxy !== undefined` and `argv.proxy` is an empty string, print `Error: --proxy requires a URL value` and call `process.exit(1)`. This catches `--proxy` passed without a value.

Do NOT add URL format validation (HTTP/SOCKS5 scheme check) — that is out of scope per the agent's discretion. The yargs `type: 'string'` already ensures non-empty strings are passed through.

Do NOT modify any other validation logic. Do NOT change the query or numeric argument checks.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/daemon.ts line 78 fork call includes `env: { ...process.env, SCRAPER_DAEMON_CHILD: '1' }`
    - src/index.ts line 37 condition is `argv.daemon && process.env.SCRAPER_DAEMON_CHILD !== '1'`
    - src/cli.ts has proxy empty-string check after line 88 that exits with error message
    - All existing tests still pass (no regressions)
  </acceptance_criteria>
  <done>Daemon child process no longer re-enters fork block; --proxy without value produces clear error; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add incrementalSaver to ScraperOptions type</name>
  <files>src/types.ts</files>
  <read_first>
    src/types.ts (lines 17-27: ScraperOptions interface)
    src/output.ts (lines 37-48: createIncrementalSaver return type signature)
  </read_first>
  <action>
**Add incrementalSaver optional property (per D-09):**

In `src/types.ts`, add `incrementalSaver?: (urls: Set<string>) => void;` to the `ScraperOptions` interface (after the `logger` field at line 26). This optional property will be wired in Plan 2.

Also add an import for `Set` if not already present — actually, `Set` is a global TypeScript type, no import needed.

Do NOT modify any other interfaces. Do NOT change existing properties.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun build --no-bundle src/types.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/types.ts ScraperOptions interface has `incrementalSaver?: (urls: Set<string>) => void` property
    - TypeScript compiles without errors
    - No existing interfaces or properties changed
  </acceptance_criteria>
  <done>ScraperOptions type includes optional incrementalSaver callback property; TypeScript compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Process | User-provided --proxy value crosses into child_process.fork env |
| Parent → Child | Environment variables propagate to forked daemon child |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | src/daemon.ts fork | critical | mitigate | SCRAPER_DAEMON_CHILD env var prevents infinite fork loops |
| T-05-02 | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in this phase |
| T-05-03 | Information Disclosure | src/cli.ts proxy validation | low | accept | Empty proxy string rejected; URL format not validated (out of scope) |
</threat_model>

<verification>
1. `bun test` passes with no regressions
2. `bun build --no-bundle src/types.ts` compiles without errors
3. Manual verification: `grep -n 'SCRAPER_DAEMON_CHILD' src/daemon.ts src/index.ts` shows env var set and checked
4. Manual verification: `grep -n 'proxy' src/cli.ts` shows empty-string validation added
</verification>

<success_criteria>
- Daemon fork infinite loop is prevented by env var check
- --proxy flag without value produces clear error message
- ScraperOptions type is ready for incremental saver integration
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/05-fix-milestone-gaps-daemon-infinite-fork-shutdown-handlers-in/05-01-SUMMARY.md` when done
</output>
