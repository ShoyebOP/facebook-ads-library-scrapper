---
status: diagnosed
phase: 08.1-add-verbose-logging-for-scroll-metrics-url-filtering-counts-
source: 08.1-01-SUMMARY.md
started: 2026-07-08T00:00:00Z
updated: 2026-07-08T00:00:00Z
---

## Current Test

number: 1
name: Per-scroll metrics logging field values
expected: |
  found = total unfiltered URLs found this scroll, filtered = dupes removed, unique = URLs saved this scroll, scrollHeight = per-scroll height
awaiting: user response

## Tests

### 1. Per-scroll metrics logging field values
expected: found = total unfiltered URLs found this scroll, filtered = dupes removed, unique = URLs saved this scroll, scrollHeight = per-scroll height
result: issue
reported: "found shows unique/saved count instead of total unfiltered. unique shows same total as found instead of unique saved. scrollHeight shows cumulative total instead of per-scroll height. filtered is correct."
severity: major

### 2. Per-scroll heartbeat logging detail
expected: Each heartbeat beat logs what happened (URLs found, scroll height, etc.) individually, not just a summary after all beats fail
result: issue
reported: "heartbeat logs only show when all 10 beats fail, does not show what happened in each beat"
severity: major

### 3. Stopping reason diagnostic snapshot
expected: Stopping reason diagnostic snapshot with reason, totalTime, scrolls, uniqueUrls, maxUrls at INFO level
result: pass
source: automated
coverage_id: D2

### 4. ScraperResult type with urls, reason, scrollCount, maxUrls fields
expected: ScraperResult type with urls, reason, scrollCount, maxUrls fields
result: pass
source: automated
coverage_id: D3

### 5. Final summary with total found, filtered, and saved counts
expected: Regardless of how script ends (normal, max-urls, SIGINT, SIGTERM), a final summary line shows total URLs found, total filtered, and total saved
result: issue
reported: "not implemented - missing feature"
severity: major

## Summary

total: 5
passed: 2
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "found field shows total unfiltered URLs found this scroll"
  status: failed
  reason: "User reported: found shows unique/saved count instead of total unfiltered"
  severity: major
  test: 1
  root_cause: "urlsFound = targetSet.size - before computes Set delta (unique-only). Misses duplicates intercepted by GraphQL but filtered out. Should be (targetSet.size - before) + filteredThisScroll for total unfiltered."
  artifacts:
    - path: "src/scraper.ts"
      issue: "Line 145: urlsFound uses Set.size delta which only counts unique additions"
  missing:
    - "Change urlsFound to (targetSet.size - before) + filteredThisScroll (compute after filteredThisScroll)"
  debug_session: .planning/debug/scroll-metrics-wrong-values.md

- truth: "unique field shows unique URLs saved this scroll"
  status: failed
  reason: "User reported: unique shows same total as found instead of unique saved"
  severity: major
  test: 1
  root_cause: "Line 158: Unique: ${runningTotal} uses same variable as Total: ${runningTotal} (both show targetSet.size cumulative). Unique should show per-scroll new unique additions."
  artifacts:
    - path: "src/scraper.ts"
      issue: "Line 158: Unique and Total use same variable runningTotal"
  missing:
    - "Change Unique: ${runningTotal} to Unique: ${targetSet.size - before}"
  debug_session: .planning/debug/scroll-metrics-wrong-values.md

- truth: "scrollHeight shows per-scroll height, not cumulative total"
  status: failed
  reason: "User reported: scrollHeight shows cumulative total instead of per-scroll height"
  severity: major
  test: 1
  root_cause: "Line 141: window.scrollY returns scroll position (distance from top), not page height. As page grows with more ads, scrollY increases appearing cumulative. Should use document.body.scrollHeight."
  artifacts:
    - path: "src/scraper.ts"
      issue: "Line 141: window.scrollY returns scroll position not page height"
  missing:
    - "Change window.scrollY to document.body.scrollHeight"
  debug_session: .planning/debug/scroll-metrics-wrong-values.md

- truth: "Heartbeat logs show per-beat detail (URLs found, scroll height, etc.)"
  status: failed
  reason: "User reported: heartbeat logs only show when all 10 beats fail, does not show what happened in each beat"
  severity: major
  test: 2
  root_cause: "Heartbeat block (lines 163-170) is nested inside if (targetSet.size === before) which only executes when no new URLs found. The else branch resets counters but never logs. Heartbeat should fire on time interval regardless of URL count."
  artifacts:
    - path: "src/scraper.ts"
      issue: "Lines 161-174: Heartbeat gated inside no-new-URLs branch"
  missing:
    - "Move heartbeat timer logic outside the if (targetSet.size === before) branch"
    - "Fire heartbeat on time interval (e.g. every 30s) regardless of whether URLs were found"
    - "Include per-beat metrics: URLs found, scroll height, filtered count, running total"
  debug_session: .planning/debug/heartbeat-per-beat-logging.md

- truth: "Final summary shows total found, filtered, and saved counts regardless of exit path"
  status: failed
  reason: "User reported: not implemented - missing feature"
  severity: major
  test: 5
  root_cause: "Three gaps: (1) ScraperResult type lacks filteredCount field, (2) No total-found metric tracked (derivable as filteredCount + uniqueUrls), (3) Shutdown handler has no summary before process.exit(0)."
  artifacts:
    - path: "src/types.ts"
      issue: "ScraperResult interface missing filteredCount field"
    - path: "src/scraper.ts"
      issue: "Return value doesnt include getFilteredCount()"
    - path: "src/index.ts"
      issue: "Stopping-reason diagnostic lacks summary; shutdown handler has no summary"
  missing:
    - "Add filteredCount: number to ScraperResult type"
    - "Include getFilteredCount() in runScraper return value"
    - "Add final summary log line after stopping-reason diagnostic"
    - "Add summary log in shutdown handler before process.exit(0)"
  debug_session: .planning/debug/final-summary-missing.md
