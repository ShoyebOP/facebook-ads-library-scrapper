---
phase: 06-milestone-v1-0-gaps-found-score-32-42-requirements-satisfied
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/index.ts
  - src/errors.ts
  - src/cli.ts
autonomous: true
requirements:
  - SETUP-05
  - SETUP-06
  - SETUP-02
  - SCRAPE-01
  - SCRAPE-02
  - CONFIG-05
  - DAEMON-04
  - SCRAPE-10
must_haves:
  truths:
    - "cosmiconfig, yargs, and zod are in dependencies (not devDependencies) — runtime imports resolve correctly"
    - "tsconfig.json moduleResolution is set to 'bun' as required by SETUP-02"
    - "Non-daemon CLI runs respond to SIGINT/SIGTERM by saving URLs and closing browser gracefully"
    - "setupShutdownHandler is removed from errors.ts — no dead export"
    - "--callback flag overrides the preset's callback URL when provided"
  artifacts:
    - package.json (dependencies fixed)
    - tsconfig.json (moduleResolution fixed)
    - src/index.ts (non-daemon shutdown wired)
    - src/errors.ts (dead shutdown handler removed)
    - src/cli.ts (--callback wired to override preset)
  key_links:
    - "index.ts shutdown handler captures browserRef and urls by closure for both daemon and non-daemon paths"
    - "cli.ts --callback override flows into webhookUrl in index.ts main()"
---

<objective>
Fix 3 integration warnings from the v1.0 milestone audit: dependency placement, module resolution mismatch, and orphaned shutdown handler. Wire non-daemon SIGINT/SIGTERM shutdown, remove dead code, and implement --callback override.

Purpose: These are the code-correctness fixes identified by the milestone audit. Without them, runtime dependencies are misclassified, TypeScript uses wrong module resolution, non-daemon runs crash on Ctrl+C without saving data, and dead exports pollute the API surface.

Output: Fixed package.json, tsconfig.json, index.ts, errors.ts, cli.ts
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-milestone-v1-0-gaps-found-score-32-42-requirements-satisfied/06-RESEARCH.md
@.planning/phases/06-milestone-v1-0-gaps-found-score-32-42-requirements-satisfied/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix package.json dependencies, tsconfig moduleResolution, and wire --callback override</name>
  <files>package.json, tsconfig.json, src/cli.ts, src/index.ts</files>
  <behavior>
    - After moving cosmiconfig/yargs/zod to dependencies, `bun install` completes without errors and `bun run tsc --noEmit` passes
    - After changing moduleResolution to "bun", `bun run tsc --noEmit` passes with no new errors
    - When --callback is provided on CLI, it overrides the preset's callback URL for webhook notification
    - When --callback is NOT provided, preset callback URL is used as before
  </behavior>
  <action>
**Step 1 — package.json dependency placement (per integration-warning: dependency-placement):**

Move `cosmiconfig`, `yargs`, and `zod` from `devDependencies` to `dependencies` in `package.json`. These are runtime imports used by `src/config.ts` and `src/cli.ts` — they must not be in devDependencies. The target state:

dependencies section should contain: cloakbrowser, cosmiconfig, p-retry, pino, playwright-core, proper-lockfile, yargs, zod

devDependencies section should contain only: @biomejs/biome, @types/bun, @types/pino

Then run `bun install` to update the lockfile.

**Step 2 — tsconfig.json moduleResolution fix (per SETUP-02):**

Change `"moduleResolution": "bundler"` to `"moduleResolution": "bun"` in `tsconfig.json`. Bun's native TS support uses its own resolver. Run `bun run tsc --noEmit` after the change to verify no type errors are introduced.

**Step 3 — Wire --callback flag to override preset callback (per SCRAPE-01/SCRAPE-02):**

In `src/index.ts`, after the preset resolution block (lines 53-58), add logic to check `argv.callback` and override `webhookUrl` if provided. The precedence should be: CLI --callback > preset callback > no webhook. This means after the preset block sets `webhookUrl`, check if `argv.callback` exists and if so, set `webhookUrl = argv.callback` and log the override. The --callback value should be treated as a full URL (not a name lookup) since the original scraper.js used a CALLBACKS registry but the TypeScript rewrite uses direct URLs via presets.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun run tsc --noEmit && bun test 2>&1 | tail -5</automated>
  </verify>
  <read_first>
    - package.json (current dependencies state)
    - tsconfig.json (current moduleResolution)
    - src/index.ts (current webhookUrl resolution logic, lines 50-58)
    - src/cli.ts (current --callback flag definition, lines 50-53)
  </read_first>
  <acceptance_criteria>
    - package.json has cosmiconfig, yargs, zod in dependencies (not devDependencies)
    - tsconfig.json has moduleResolution set to "bun"
    - `bun run tsc --noEmit` exits cleanly
    - `bun test` passes all existing tests
    - When CLI --callback flag is provided, it overrides the preset's callback URL
    - When --callback is not provided, behavior is unchanged
  </acceptance_criteria>
  <done>Dependencies are correctly placed for runtime, TypeScript compiles with bun module resolution, and --callback flag overrides preset callback URL</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire non-daemon shutdown handlers and remove dead setupShutdownHandler from errors.ts</name>
  <files>src/index.ts, src/errors.ts</files>
  <behavior>
    - Non-daemon CLI runs (without --daemon flag) respond to SIGINT by saving collected URLs to disk and closing the browser
    - Non-daemon CLI runs respond to SIGTERM with the same graceful behavior
    - The shutdown handler does NOT fire twice if SIGINT arrives during shutdown
    - setupShutdownHandler function and ShutdownDeps interface are removed from errors.ts
    - All existing tests still pass after removing the dead code
  </behavior>
  <action>
**Step 1 — Wire non-daemon shutdown handlers in index.ts (per broken-flow: non-daemon-shutdown):**

In `src/index.ts`, the shutdown wiring block at lines 105-120 currently only handles the daemon case (`SCRAPER_DAEMON_CHILD === '1'`). Extend this block to handle BOTH daemon and non-daemon paths. The structure should be:

After `const urls = await runScraper(options);` (line 103), the code should:

1. Define the shutdownDeps object with saveState (calls saveUrlsToFile) and cleanup (closes browserRef if present) — this is already done inside the daemon block
2. Move the shutdownDeps definition OUTSIDE the if-block so both paths share it
3. Keep the daemon path calling `setupDaemonShutdown(shutdownDeps)`
4. Add an else branch for non-daemon: register SIGINT and SIGTERM handlers directly using `process.on()` with a `shuttingDown` guard flag (same pattern as `setupDaemonShutdown` in `src/daemon.ts`). On signal: log the signal, call `shutdownDeps.saveState()`, call `shutdownDeps.cleanup()`, then `process.exit(0)`.

Important: The `urls` variable is the Set returned by `runScraper`. The saveState closure captures it by reference. The `browserRef` is captured via the `onBrowserReady` callback. Both are already set up correctly — the non-daemon handler just needs to use them.

**Step 2 — Remove dead setupShutdownHandler from errors.ts:**

Delete the following from `src/errors.ts`:
- The `ShutdownDeps` interface (lines 112-116)
- The `setupShutdownHandler` function (lines 118-150)
- The section comment `// --- Graceful shutdown handler (SCRAPE-10) ---` (line 110)

Keep everything else in errors.ts (withTimeout, classifyError, withRetry, ErrorType).

**Step 3 — Verify no imports reference setupShutdownHandler:**

Grep for `setupShutdownHandler` across the entire `src/` directory to confirm it is not imported anywhere. The research confirmed it is dead code — exported but never called.

**Step 4 — Run tests to confirm no regressions:**

Run `bun test` to verify all existing tests still pass after removing the dead code.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test 2>&1 | tail -5</automated>
  </verify>
  <read_first>
    - src/index.ts (current shutdown wiring at lines 105-120)
    - src/errors.ts (dead setupShutdownHandler at lines 110-150)
    - src/daemon.ts (setupDaemonShutdown pattern to replicate for non-daemon)
  </read_first>
  <acceptance_criteria>
    - src/index.ts wires SIGINT/SIGTERM for BOTH daemon and non-daemon paths
    - Non-daemon shutdown saves URLs and closes browser before exiting
    - setupShutdownHandler and ShutdownDeps are removed from src/errors.ts
    - grep for setupShutdownHandler in src/ returns zero results
    - All existing tests pass
  </acceptance_criteria>
  <done>Non-daemon CLI runs gracefully shutdown on SIGINT/SIGTERM (save data + close browser), dead shutdown handler removed from errors.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → pipeline | User provides --callback URL that becomes webhook destination |
| Signal handler → process | SIGINT/SIGTERM triggers graceful shutdown with data save |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | --callback CLI arg | low | accept | --callback overrides webhook URL; user controls their own webhook endpoint — no injection risk since URL is passed to http.request which validates format |
| T-06-02 | Information Disclosure | Shutdown handler | low | accept | Shutdown logs URL count and file path; no sensitive data exposed in logs |
| T-06-03 | Denial of Service | Signal handler race | low | mitigate | shuttingDown guard flag prevents double-execution; handler checks browserRef before closing |
</threat_model>

<verification>
1. `bun run tsc --noEmit` passes (moduleResolution fix)
2. `bun test` passes all existing tests
3. `grep -r "setupShutdownHandler" src/` returns zero results
4. package.json has cosmiconfig, yargs, zod in dependencies
5. tsconfig.json has moduleResolution "bun"
6. Non-daemon shutdown handler saves URLs and closes browser on SIGINT
</verification>

<success_criteria>
- All 3 integration warnings from milestone audit resolved
- Non-daemon shutdown flow works end-to-end
- Dead code removed
- --callback flag functional
- Zero test regressions
</success_criteria>

<output>
Create `.planning/phases/06-milestone-v1-0-gaps-found-score-32-42-requirements-satisfied/06-01-SUMMARY.md` when done
</output>
