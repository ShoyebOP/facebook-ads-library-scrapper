---
phase: 02-core-scraper-engine
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/errors.ts
  - src/browser.ts
  - src/extractor.ts
  - src/logger.ts
  - tests/errors.test.ts
autonomous: true
requirements: [ERROR-01, ERROR-02, ERROR-03, ERROR-04, ERROR-05, SCRAPE-10]
user_setup: []

must_haves:
  truths:
    - "Error classification distinguishes transient, permanent, browser, and extraction errors"
    - "Exponential backoff retry via p-retry wraps transient errors"
    - "All catch blocks log errors (no silent error swallowing)"
    - "Proxy credentials sanitized in all log output (D-21)"
    - "Graceful shutdown handlers save data and close browser on SIGINT/SIGTERM"
  artifacts:
    - src/errors.ts
    - tests/errors.test.ts
  key_links:
    - "classifyError maps error messages to ErrorType enum"
    - "withRetry wraps p-retry with exponential backoff config"
    - "setupShutdownHandler registers SIGINT/SIGTERM and closes browser"
---

<objective>
Implement error handling foundation: classification, retry, and graceful shutdown.

Purpose: The scraper encounters network timeouts, browser crashes, and malformed responses. Without error classification, it retries permanent errors indefinitely (known bug from original scraper.js). This plan adds structured error handling that classifies errors, retries only transient ones, and ensures graceful shutdown saves partial data.

Output: Working errors.ts with withTimeout, classifyError, withRetry, and setupShutdownHandler. Enhanced browser.ts and extractor.ts with proper error handling.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-scraper-engine/02-CONTEXT.md
@.planning/phases/02-core-scraper-engine/02-RESEARCH.md
@.planning/phases/02-core-scraper-engine/02-PATTERNS.md
@src/browser.ts
@src/extractor.ts
@src/logger.ts
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement error classification and retry wrapper</name>
  <files>src/errors.ts, tests/errors.test.ts</files>
  <read_first>
    - scraper.js:79-84 (original withTimeout function)
    - src/types.ts (ErrorCategory type)
    - src/logger.ts (for Logger type)
  </read_first>
  <behavior>
    - Test: withTimeout resolves if promise completes before deadline
    - Test: withTimeout rejects with 'Timed out after Xms' if exceeded
    - Test: classifyError returns 'transient' for timeout/network errors
    - Test: classifyError returns 'browser' for browser/crash errors
    - Test: classifyError returns 'extraction' for json/parse errors
    - Test: classifyError returns 'permanent' for other errors
    - Test: withRetry calls fn at least once
    - Test: withRetry retries on transient errors up to max retries
    - Test: withRetry throws immediately on permanent errors
    - Test: withRetry logs attempt number and retries left
  </behavior>
  <action>
    Create src/errors.ts implementing error handling foundation. Per D-12: withTimeout(promise, ms) using Promise.race pattern from scraper.js:79-84. Per D-19: classifyError(error) mapping error.message.toLowerCase() to ErrorType enum ('transient' for timeout/network, 'browser' for browser/crash, 'extraction' for json/parse, 'permanent' for others). Per ERROR-03: withRetry(fn, options) wrapping p-retry with exponential backoff (retries: 3, factor: 2, minTimeout: 1000, maxTimeout: 10000, randomize: true). Per ERROR-04: onFailedAttempt callback logs warning with attempt number and retries left. Import pRetry from 'p-retry'. Export withTimeout, classifyError, ErrorType, withRetry.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/errors.test.ts</automated>
  </verify>
  <done>errors.ts implements withTimeout, classifyError, withRetry; all error tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement graceful shutdown and wire error handling</name>
  <files>src/scraper.ts, tests/errors.test.ts</files>
  <read_first>
    - src/errors.ts (withTimeout, classifyError, withRetry)
    - src/browser.ts (launchBrowser)
    - src/extractor.ts (setupGraphQLInterceptor)
    - src/logger.ts (createChildLogger)
    - scraper.js:162-174 (original shutdown handler)
  </read_first>
  <behavior>
    - Test: setupShutdownHandler registers SIGINT listener
    - Test: setupShutdownHandler registers SIGTERM listener
    - Test: shutdown handler calls saveUrls before exit
    - Test: shutdown handler closes browser
    - Test: shutdown handler prevents double-execution via shuttingDown flag
    - Test: browser.ts uses withRetry for launch (transient errors retried)
    - Test: browser.ts logs error and re-throws on permanent failure
  </behavior>
  <action>
    Add setupShutdownHandler to src/errors.ts. Per SCRAPE-10: register process.on('SIGINT') and process.on('SIGTERM'). Use shuttingDown flag to prevent double-execution. Shutdown sequence: log 'Shutting down gracefully...', call saveUrls(profileUrls), try browser.close() with error logging, process.exit(0). Per D-07: browser.ts launch wrapped with withRetry for transient errors; permanent errors log and re-throw. Wire withTimeout into extractor.ts response.json() call (replace local helper). Ensure all catch blocks log via logger (ERROR-04). Export setupShutdownHandler from errors.ts.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/errors.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>Graceful shutdown saves data and closes browser, error handling wired into browser and extractor, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process → OS | Signal handlers must not deadlock or double-exit |
| Error → Retry | Only transient errors should be retried; permanent errors must fail fast |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Denial of Service | Infinite retry loop | high | mitigate | classifyError distinguishes transient vs permanent; p-retry respects max retries |
| T-02-07 | Information Disclosure | Error messages in logs | low | accept | Error messages are non-sensitive; proxy credentials redacted by pino |
| T-02-08 | Elevation of Privilege | Double exit in shutdown | medium | mitigate | shuttingDown flag prevents re-entry; process.exit(0) only after cleanup |
</threat_model>

<verification>
- `bun test tests/errors.test.ts` passes
- `bun run tsc --noEmit` exits 0
- `bunx biome check ./src` exits 0
</verification>

<success_criteria>
- Error classification correctly categorizes 4 error types
- Retry wrapper uses exponential backoff for transient errors
- Graceful shutdown saves data and closes browser
- All catch blocks log errors (no silent swallowing)
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/02-core-scraper-engine/02-03-SUMMARY.md` when done
</output>
