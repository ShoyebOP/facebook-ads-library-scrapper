---
phase: 02-core-scraper-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser.ts
  - src/logger.ts
  - src/types.ts
  - package.json
  - bun.lock
autonomous: true
requirements: [SCRAPE-03, SCRAPE-04, SCRAPE-09, ERROR-01]
user_setup: []

must_haves:
  truths:
    - "Browser launches with stealth configuration (humanize: true, humanPreset: 'careful')"
    - "Browser supports optional HTTP/SOCKS5 proxy with URL-embedded credentials"
    - "Browser closes automatically after scrape completes or on error"
    - "Structured logging with pino outputs JSON with redacted proxy credentials"
  artifacts:
    - src/browser.ts
    - src/logger.ts
    - src/types.ts
  key_links:
    - "cloakbrowser launch() receives stealth options from BrowserOptions"
    - "pino logger redacts proxy field in all log output"
---

<objective>
Implement the browser layer with cloakbrowser stealth integration and structured logging foundation.

Purpose: Establish the anti-detection browser automation that Facebook Ads Library scraping depends on. Without stealth configuration, the scraper gets blocked immediately. This plan also installs pino for structured logging, which all subsequent plans use.

Output: Working browser.ts that launches a stealth browser, logger.ts with pino configuration, and types.ts with shared interfaces.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-scraper-engine/02-CONTEXT.md
@.planning/phases/02-core-scraper-engine/02-RESEARCH.md
@.planning/phases/02-core-scraper-engine/02-PATTERNS.md
@src/browser.ts
@src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create types</name>
  <files>package.json, bun.lock, src/types.ts</files>
  <read_first>
    - src/config.ts (to understand existing type patterns)
    - src/index.ts (to understand CliArgs type)
  </read_first>
  <behavior>
    - Test: pino is installed and importable
    - Test: p-retry is installed and importable
    - Test: types.ts exports BrowserOptions with headless, proxy?, locale?, timezone?, logger fields
    - Test: types.ts exports ScraperOptions with query, maxUrls?, maxNoNewScrolls, headless, proxy?, locale?, timezone?, logger fields
  </behavior>
  <action>
    Install pino and p-retry as dependencies (per D-20: console output only, pino provides structured JSON). Create src/types.ts with shared TypeScript interfaces. BrowserOptions must include: headless (boolean), proxy? (string, URL-embedded format per D-03), locale? (string), timezone? (string), logger (pino.Logger). ScraperOptions must include: query (string), maxUrls? (number), maxNoNewScrolls (number), headless (boolean), proxy? (string), locale? (string), timezone? (string), logger (pino.Logger). Export ErrorCategory type as 'transient' | 'permanent' | 'browser' | 'extraction' (per D-19). Preserve existing CliArgs type in src/index.ts.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/setup.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>pino and p-retry installed, types.ts created with BrowserOptions, ScraperOptions, and ErrorCategory types, TypeScript compiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement logger with pino</name>
  <files>src/logger.ts, tests/logger.test.ts</files>
  <read_first>
    - src/types.ts (to import Logger type)
  </read_first>
  <behavior>
    - Test: createLogger returns a pino logger instance
    - Test: logger respects LOG_LEVEL environment variable
    - Test: logger redacts 'proxy' field in log output (D-21)
    - Test: createChildLogger returns logger with module field
  </behavior>
  <action>
    Create src/logger.ts implementing pino structured logging (ERROR-01). Export createLogger(level?: string) that returns pino instance with redact: ['proxy', '*.proxy'] for proxy credential sanitization (D-21). Export createChildLogger(parent, module) that creates child logger with module field. Default level 'info' unless LOG_LEVEL env var set. Use pino's built-in JSON transport for structured output.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/logger.test.ts</automated>
  </verify>
  <done>logger.ts created, proxy credentials redacted in logs, child loggers work, all logger tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement browser launcher with stealth config</name>
  <files>src/browser.ts, tests/browser.test.ts</files>
  <read_first>
    - src/types.ts (BrowserOptions interface)
    - src/logger.ts (createLogger, createChildLogger)
    - scraper.js:124-134 (original browser launch pattern)
  </read_first>
  <behavior>
    - Test: launchBrowser calls cloakbrowser launch with headless option
    - Test: launchBrowser sets humanize: true and human_preset: 'careful' (D-02, D-04)
    - Test: launchBrowser includes proxy when provided (D-03)
    - Test: launchBrowser includes locale and timezone when provided (D-06)
    - Test: launchBrowser logs error and re-throws on failure (D-07)
    - Test: launchBrowser returns Browser instance on success
  </behavior>
  <action>
    Implement src/browser.ts replacing the stub. Export launchBrowser(options: BrowserOptions) that calls cloakbrowser's launch() with stealth configuration. Per D-02: humanize: true, human_preset: 'careful'. Per D-04: stealth_args: true. Per D-05: headless from options (default true). Per D-03: proxy URL-embedded format passed directly to launch. Per D-06: locale and timezone from options with fallback to 'en-US' and 'Asia/Dhaka'. Per D-07: wrap launch in try-catch, log error details via browserLogger.error(), re-throw. Import launch from 'cloakbrowser' and Browser type from 'playwright-core'. Create browserLogger via createChildLogger(logger, 'browser').
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/browser.test.ts</automated>
  </verify>
  <done>browser.ts launches cloakbrowser with stealth config, proxy support, error logging, all browser tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Facebook | Anti-detection bypass required; stealth config is critical |
| Proxy → Browser | Credentials embedded in URL; must not leak in logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | proxy field in logs | high | mitigate | pino redact config sanitizes 'proxy' and '*.proxy' fields |
| T-02-02 | Elevation of Privilege | Browser fingerprint detection | high | mitigate | cloakbrowser stealth_args: true, humanize: true, human_preset: 'careful' |
| T-02-03 | Tampering | Browser launch failure | medium | mitigate | Log error details and re-throw per D-07; caller handles retry |
</threat_model>

<verification>
- `bun run tsc --noEmit` exits 0
- `bun test tests/browser.test.ts` passes
- `bun test tests/logger.test.ts` passes
- `bunx biome check ./src` exits 0
</verification>

<success_criteria>
- Browser launches with stealth configuration (humanize, humanPreset, stealth_args)
- Proxy credentials redacted in all log output
- Structured JSON logging via pino
- All tests pass, TypeScript compiles, Biome lints cleanly
</success_criteria>

<output>
Create `.planning/phases/02-core-scraper-engine/02-01-SUMMARY.md` when done
</output>
