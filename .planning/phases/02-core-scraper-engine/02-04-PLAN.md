---
phase: 02-core-scraper-engine
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/scraper.ts
  - src/index.ts
  - tests/scraper.test.ts
autonomous: true
requirements: [SCRAPE-07, SCRAPE-08, SCRAPE-10]
user_setup: []

must_haves:
  truths:
    - "Scraper scrolls through Facebook Ads Library page loading more ads"
    - "DOM cleanup removes processed ad cards above viewport after each scroll"
    - "Scroll loop stops after 10 consecutive scrolls with no new URLs (D-16)"
    - "Scroll failures retry with 1s delay (D-17)"
    - "End-to-end pipeline works: CLI → Browser → Extraction → Scroll → Output"
  artifacts:
    - src/scraper.ts
    - src/index.ts
    - tests/scraper.test.ts
  key_links:
    - "scroll loop integrates with GraphQL interceptor for URL collection"
    - "DOM cleanup uses [role='row'] selector to remove old elements"
    - "Pipeline orchestrator wires all components together"
---

<objective>
Implement the scroll loop, DOM cleanup, and end-to-end pipeline integration.

Purpose: The final vertical slice that brings all components together. After this plan, the scraper can navigate to Facebook Ads Library, scroll through pages collecting ads, extract profile URLs from GraphQL responses, clean up DOM to prevent memory leaks, and save results. This completes the core scraper engine.

Output: Working scraper.ts with scroll loop and DOM cleanup, updated index.ts wiring all components, and end-to-end tests.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-scraper-engine/02-CONTEXT.md
@.planning/phases/02-core-scraper-engine/02-RESEARCH.md
@.planning/phases/02-core-scraper-engine/02-PATTERNS.md
@src/browser.ts
@src/extractor.ts
@src/errors.ts
@src/logger.ts
@src/types.ts
@src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement scroll loop with DOM cleanup</name>
  <files>src/scraper.ts, tests/scraper.test.ts</files>
  <read_first>
    - src/browser.ts (launchBrowser)
    - src/extractor.ts (setupGraphQLInterceptor, extractProfileUrls)
    - src/errors.ts (withTimeout, setupShutdownHandler)
    - src/logger.ts (createChildLogger)
    - src/types.ts (ScraperOptions)
    - scraper.js:185-228 (original scroll loop pattern)
    - scraper.js:162-174 (original shutdown handler)
  </read_first>
  <behavior>
    - Test: runScraper launches browser and navigates to Facebook Ads Library
    - Test: runScraper sets up GraphQL interceptor on page
    - Test: runScraper scrolls page to bottom
    - Test: runScraper waits 2500ms between scrolls (D-14)
    - Test: runScraper cleans up DOM by removing rows above viewport (D-15)
    - Test: runScraper stops after 10 consecutive scrolls with no new URLs (D-16)
    - Test: runScraper stops when maxUrls reached
    - Test: runScraper retries failed scrolls with 1s delay (D-17)
    - Test: runScraper closes browser on completion (D-04)
    - Test: runScraper returns Set of collected URLs
  </behavior>
  <action>
    Implement src/scraper.ts replacing the stub. Export runScraper(options: ScraperOptions): Promise&lt;Set&lt;string&gt;&gt;. Per D-14: fixed 2500ms interval between scrolls. Per D-15: after each scroll, remove DOM elements with [role='row'] selector whose rect.bottom is below viewport top minus 500px. Per D-16: hardcoded threshold of 10 consecutive scrolls with no new URLs (no CLI flag). Per D-17: on scroll failure, log error, wait 1000ms, continue. Per D-04: close browser in finally block. Use withTimeout for scroll operations (10s). Use withTimeout for DOM cleanup (10s). Use scrollLogger = createChildLogger(logger, 'scroll'). Initialize profileUrls Set, call setupGraphQLInterceptor, loop with scroll + cleanup, return profileUrls. Navigate to facebook.com/ads/library/ with search query parameter.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/scraper.test.ts</automated>
  </verify>
  <done>scraper.ts implements scroll loop with DOM cleanup, stop criteria, retry on failure, all scraper tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire end-to-end pipeline in index.ts</name>
  <files>src/index.ts, tests/scraper.test.ts</files>
  <read_first>
    - src/scraper.ts (runScraper)
    - src/browser.ts (launchBrowser)
    - src/config.ts (loadConfig, resolvePreset)
    - src/logger.ts (createLogger)
    - src/types.ts (ScraperOptions)
    - src/index.ts (existing pipeline stub)
  </read_first>
  <behavior>
    - Test: main() creates logger with pino
    - Test: main() loads config and resolves preset if provided
    - Test: main() constructs ScraperOptions from CliArgs
    - Test: main() calls runScraper with options
    - Test: main() logs completion with URL count
    - Test: main() returns Set of URLs (for Phase 3 output wiring)
  </behavior>
  <action>
    Update src/index.ts to wire the complete pipeline. Per D-20: create pino logger via createLogger(). Load config via loadConfig(), resolve preset if argv.preset provided. Construct ScraperOptions from CliArgs: query, maxUrls, maxNoNewScrolls, headless, proxy, locale, timezone, logger. Call runScraper(options) and capture returned URL Set. Log completion with URL count. Return URL Set for Phase 3 output wiring. Remove stub console.log statements. Keep existing CliArgs type and main function signature.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/scraper.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>index.ts wires complete pipeline: CLI → Config → Logger → Scraper → URLs, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scraper → Facebook | Navigation to ads library; must handle rate limiting |
| DOM → Memory | Unbounded DOM growth causes memory leaks; cleanup is critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Denial of Service | Memory leak from DOM | high | mitigate | DOM cleanup removes rows above viewport after each scroll |
| T-02-10 | Denial of Service | Infinite scroll loop | medium | mitigate | Hardcoded threshold of 10 no-new-URL scrolls (D-16) |
| T-02-11 | Tampering | Facebook rate limiting | medium | accept | Proxy support (D-03) and humanization (D-04) mitigate; rate limit response is handled by retry |
</threat_model>

<verification>
- `bun test tests/scraper.test.ts` passes
- `bun run tsc --noEmit` exits 0
- `bunx biome check ./src` exits 0
- `bun run src/cli.ts --query test` runs end-to-end (stub output acceptable)
</verification>

<success_criteria>
- Scroll loop navigates Facebook Ads Library and collects URLs
- DOM cleanup prevents memory leaks
- Stop criteria work (10 no-new-URL scrolls or maxUrls reached)
- End-to-end pipeline: CLI → Browser → Extraction → Scroll → URLs
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/02-core-scraper-engine/02-04-SUMMARY.md` when done
</output>
