---
phase: 02-core-scraper-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/extractor.ts
  - tests/extractor.test.ts
autonomous: true
requirements: [SCRAPE-05, SCRAPE-06, ERROR-04]
user_setup: []

must_haves:
  truths:
    - "GraphQL responses are intercepted via page.on('response') event listener"
    - "Profile URLs (page_profile_uri) are extracted from nested GraphQL JSON"
    - "Duplicate URLs are deduplicated using in-memory Set"
    - "Response parsing timeout is 15 seconds; slow responses are skipped"
    - "Malformed responses log warning and continue to next response"
  artifacts:
    - src/extractor.ts
    - tests/extractor.test.ts
  key_links:
    - "page.on('response') filters for URLs containing 'graphql'"
    - "extractProfileUrls recursively traverses nested JSON objects"
    - "withTimeout wraps response.json() with 15s deadline"
---

<objective>
Implement GraphQL response interception and profile URL extraction.

Purpose: The core data extraction layer that captures Facebook Ads Library profile URLs from intercepted GraphQL responses. Without this, the browser loads pages but extracts nothing. This plan wires extraction into the browser's response event system.

Output: Working extractor.ts with setupGraphQLInterceptor and extractProfileUrls functions, plus comprehensive tests.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-scraper-engine/02-CONTEXT.md
@.planning/phases/02-core-scraper-engine/02-RESEARCH.md
@.planning/phases/02-core-scraper-engine/02-PATTERNS.md
@src/browser.ts
@src/logger.ts
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement profile URL extraction logic</name>
  <files>src/extractor.ts, tests/extractor.test.ts</files>
  <read_first>
    - scraper.js:60-76 (original extractProfileUrls function)
    - src/types.ts (for Logger type import)
  </read_first>
  <behavior>
    - Test: extracts page_profile_uri from flat object
    - Test: extracts URLs from nested arrays (edges → node pattern)
    - Test: extracts URLs from deeply nested objects
    - Test: deduplicates URLs using Set (D-11)
    - Test: handles null/undefined input gracefully
    - Test: handles non-object input gracefully
  </behavior>
  <action>
    Create src/extractor.ts with extractProfileUrls(obj: unknown, urls: Set&lt;string&gt;): void. Per D-10: use recursive traversal (proven pattern from scraper.js:60-76). Check if obj has 'page_profile_uri' string field, add to urls Set. If array, recurse on each element. If object, recurse on each value. Handle null/undefined by returning early. Export the function for testing and use by setupGraphQLInterceptor.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/extractor.test.ts</automated>
  </verify>
  <done>extractProfileUrls handles all nested JSON patterns, deduplicates URLs, all extraction tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GraphQL response interceptor</name>
  <files>src/extractor.ts, tests/extractor.test.ts</files>
  <read_first>
    - src/extractor.ts (existing extractProfileUrls)
    - src/logger.ts (createChildLogger)
    - src/types.ts (Logger type)
    - scraper.js:139-160 (original response interception pattern)
  </read_first>
  <behavior>
    - Test: setupGraphQLInterceptor registers page.on('response') listener
    - Test: interceptor filters responses where URL contains 'graphql' (D-09)
    - Test: interceptor filters responses with status 200
    - Test: interceptor calls extractProfileUrls with parsed JSON
    - Test: interceptor wraps response.json() with 15s timeout (D-12)
    - Test: interceptor logs warning on timeout and continues (D-13)
    - Test: interceptor logs debug on non-JSON response and continues (D-13)
    - Test: interceptor logs total URL count after extraction
  </behavior>
  <action>
    Add setupGraphQLInterceptor(page: Page, profileUrls: Set&lt;string&gt;, logger: Logger) to src/extractor.ts. Per D-08: use page.on('response') listener (simpler for data extraction than route interception). Per D-09: filter by response.url().includes('graphql') and response.status() === 200. Per D-12: wrap response.json() with withTimeout(response.json(), 15000). Per D-13: catch errors, log warning if timeout ('Timed out' in message), log debug otherwise, continue to next response. Import Page and Response from 'playwright-core'. Import withTimeout from './errors.js' (will be created in Plan 02-03; for now, create a local withTimeout helper that will be replaced). Use extractLogger = createChildLogger(logger, 'extractor').
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test tests/extractor.test.ts</automated>
  </verify>
  <done>GraphQL interceptor filters responses, extracts URLs, handles timeouts, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Facebook GraphQL → Extractor | Untrusted JSON responses; must handle malformed data |
| Extractor → Set | In-memory state; no persistence (Phase 3 handles file output) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | Malformed GraphQL response | medium | mitigate | Log warning and continue per D-13; never crash on bad data |
| T-02-05 | Denial of Service | Slow response.json() | medium | mitigate | withTimeout at 15s per D-12; skip slow responses |
</threat_model>

<verification>
- `bun test tests/extractor.test.ts` passes
- `bun run tsc --noEmit` exits 0
- `bunx biome check ./src` exits 0
</verification>

<success_criteria>
- GraphQL responses intercepted via page.on('response')
- Profile URLs extracted from nested JSON structures
- Timeout handling skips slow responses
- All extraction tests pass
</success_criteria>

<output>
Create `.planning/phases/02-core-scraper-engine/02-02-SUMMARY.md` when done
</output>
