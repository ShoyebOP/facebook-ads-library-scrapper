# Phase 01.1: Entry Point & Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-05
**Phase:** 01.1-make-a-entry-point-or-move-the-cli-to-the-root-so-i-don-t-ha
**Areas discussed:** Entry point location, Custom ad library URL, Old file cleanup, README content

---

## Entry point location

| Option | Description | Selected |
|--------|-------------|----------|
| bin script in package.json (Recommended) | Add "bin" field pointing to src/cli.ts. Run via `npx facebook-scraper` or link globally. Keeps cli.ts in src/. | ✓ |
| Wrapper script at root | Create scraper.ts at root that imports and runs src/cli.ts. Run via `bun scraper.ts`. Simple but adds a file. | |
| Move cli.ts to root | Move src/cli.ts to project root. Run via `bun cli.ts`. Breaks src/ module boundaries. | |

**User's choice:** bin script in package.json
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| facebook-scraper (Recommended) | Descriptive, matches project name. `npx facebook-scraper "query"` | |
| fb-scraper | Shorter, easier to type. `npx fb-scraper "query"` | |
| fasl | Very short acronym (Facebook Ads Library Scraper). `npx fasl "query"` | |

**User's choice:** fb-ad-scraper
**Notes:** User selected a different name than options provided

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, add npm script too (Recommended) | Add "start" script in package.json: `bun src/cli.ts`. Users can `bun start "query"` from project root. | ✓ |
| No, bin only | Only global/npx installation. Users must install globally or use full path. | |

**User's choice:** Yes, add npm script too
**Notes:** User asked "will the flags work?" — confirmed yargs parses process.argv regardless of invocation method

| Option | Description | Selected |
|--------|-------------|----------|
| "start": "bun src/cli.ts" (Recommended) | Standard `bun start "query"` invocation. Simple and conventional. | ✓ |
| "dev": "bun src/cli.ts" | Use "dev" instead of "start" to distinguish from production. `bun run dev "query"`. | |

**User's choice:** "start": "bun src/cli.ts"
**Notes:** None

---

## Custom ad library URL

| Option | Description | Selected |
|--------|-------------|----------|
| config.json field (Recommended) | Add `adLibraryUrl` field to each preset. Falls back to default Facebook Ads Library URL if empty. Clean, single source of truth. | |
| CLI flag --url | Add `--url` flag to override per-run. Useful for one-off overrides but adds CLI complexity. | |
| Both config + CLI flag | Config sets default per preset, CLI flag overrides. Most flexible but more code. | ✓ |

**User's choice:** Both config + CLI flag
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| CLI flag overrides config (Recommended) | CLI flag takes precedence. Config provides default, CLI is explicit override. Standard pattern. | ✓ |
| Config overrides CLI flag | Config takes precedence. Unusual but keeps preset behavior consistent. | |

**User's choice:** CLI flag overrides config
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Current hardcoded URL (Recommended) | Use the existing BASE_URL from scraper.js as default. Backward compatible. | ✓ |
| No default, require URL | Error if no URL provided. Forces explicit configuration. | |

**User's choice:** Current hardcoded URL
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| {query} (Recommended) | Mustache-style. `https://...?q={query}`. Clear, common pattern. | ✓ |
| ${query} | Shell-style. `https://...?q=${query}`. Familiar but could conflict with shell expansion. | |
| {{query}} | Jinja-style. `https://...?q={{query}}`. Template-like but verbose. | |

**User's choice:** {query}
**Notes:** User clarified they want full URL with all parameters, not just base URL. Additional parameters after {query} are preserved — URL encoding handled automatically.

---

## Old file cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| scraper.js only (Recommended) | Remove the original monolithic JS file. Keep example_page_code.html as reference. | ✓ |
| scraper.js + example_page_code.html | Remove both. Clean slate — new TypeScript modules replace everything. | |
| scraper.js + example_page_code.html + README.md | Remove all old files. Will create fresh README in this phase. | |

**User's choice:** scraper.js only
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, update all references (Recommended) | Update package.json scripts, .gitignore, any docs that reference scraper.js. Keep things consistent. | ✓ |
| No, just delete the file | Delete scraper.js and let broken references be fixed later. | |

**User's choice:** Yes, update all references
**Notes:** None

---

## README content

| Option | Description | Selected |
|--------|-------------|----------|
| Full docs (Recommended) | Installation, usage, configuration, examples, CLI flags, presets. Comprehensive reference. | ✓ |
| Minimal | Quick start only: install, basic usage, link to config docs. Keep it short. | |
| Essentials + config | Installation, usage, and detailed config reference. Balance between minimal and full. | |

**User's choice:** Full docs
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| All standard sections (Recommended) | Title, description, installation, quick start, usage, configuration, CLI flags, examples, license. | ✓ |
| Minimal + config | Title, installation, usage, configuration reference. Skip examples and description. | |
| Custom mix | Let me specify which sections to include. | |

**User's choice:** All standard sections
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, with all fields (Recommended) | Show complete config.json with presets, adLibraryUrl, webhook, proxy, etc. Users can copy-paste. | ✓ |
| Yes, minimal example | Show basic config with just query and preset. Link to full reference elsewhere. | |
| No config example | Skip config example. Users read config.ts for schema. | |

**User's choice:** Yes, with all fields
**Notes:** None

---

## the agent's Discretion

- Exact bin field format in package.json
- URL encoding implementation details (which Bun API to use)
- README markdown structure and formatting choices
- Whether to add config.json schema documentation in README or separate file

## Deferred Ideas

None — discussion stayed within phase scope
