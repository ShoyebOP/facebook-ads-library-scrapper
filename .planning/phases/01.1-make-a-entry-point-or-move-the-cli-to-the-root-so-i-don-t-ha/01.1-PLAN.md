---
phase: 01.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/config.ts
  - src/cli.ts
  - src/types.ts
  - src/scraper.ts
  - src/index.ts
  - config.example.json
  - scraper.js
  - README.md
  - .gitignore
autonomous: true
requirements: []
must_haves:
  truths:
    - User can run `bun start "query"` from project root without specifying src/cli.ts
    - User can run `fb-ad-scraper "query"` after global install (bin field)
    - User can set adLibraryUrl in config preset with {query} placeholder
    - User can override adLibraryUrl via --url CLI flag
    - CLI flag takes precedence over config preset, which takes precedence over default
    - scraper.js is removed from project root
    - README.md contains complete documentation
  artifacts:
    - package.json (with bin field and updated scripts)
    - src/config.ts (with adLibraryUrl in PresetSchema)
    - src/cli.ts (with --url flag)
    - src/scraper.ts (using dynamic URL from options)
    - config.example.json (with adLibraryUrl example)
    - README.md (comprehensive documentation)
  key_links:
    - CLI --url flag → ScraperOptions.url → scraper.ts searchUrl construction
    - Config preset adLibraryUrl → ScraperOptions.url → scraper.ts searchUrl construction
    - package.json bin → src/cli.ts shebang
---

<objective>
Make the scraper runnable from project root via `bun start` or `fb-ad-scraper` command, add custom ad library URL support per preset with CLI override, remove legacy scraper.js, and create comprehensive project documentation.

Purpose: CLI usability improvement — users should not need to specify `src/cli.ts` path, and should be able to customize the Facebook Ads Library URL per preset.
Output: Updated package.json with bin field, extended config schema with adLibraryUrl, CLI --url flag, cleaned up legacy files, and complete README.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-make-a-entry-point-or-move-the-cli-to-the-root-so-i-don-t-ha/01.1-CONTEXT.md
@src/cli.ts
@src/config.ts
@src/scraper.ts
@src/index.ts
@src/types.ts
@package.json
@config.example.json
@scraper.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry Point & Config Schema Updates</name>
  <files>package.json, src/config.ts, src/cli.ts, src/types.ts, config.example.json</files>
  <action>
**Per D-01, D-02, D-03, D-04:** Update package.json to add bin field and scripts:
- Add `"bin": { "fb-ad-scraper": "./src/cli.ts" }` to package.json
- Keep existing `"start": "bun run src/cli.ts"` script (per D-03)
- Ensure `"type": "module"` is preserved

**Per D-05, D-09, D-010:** Update src/config.ts Zod schema:
- Add `adLibraryUrl` field to `PresetSchema` as optional string with `.url()` validation
- The field stores a full URL with `{query}` placeholder (e.g., `https://www.facebook.com/ads/library/?active_status=active&ad_type=all&country=ALL&q={query}`)
- Keep existing `callback` field
- Update `Config` and `Preset` type exports

**Per D-06:** Update src/cli.ts to add --url flag:
- Add `url` option to yargs options: `{ type: 'string', describe: 'Override ad library URL for this run' }`
- Add `url` to CliArgs type in src/index.ts

**Per D-05, D-014:** Update config.example.json:
- Add `adLibraryUrl` field to the leadgen preset with example URL containing `{query}` placeholder
- Keep existing `callback` field

**Per D-012:** Update .gitignore:
- Remove any reference to `scraper.js` if present (currently not referenced, but verify)

Do NOT modify src/scraper.ts or src/index.ts in this task — those are wired in Task 2.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && cat package.json | grep -c '"fb-ad-scraper"' && bunx biome check src/config.ts src/cli.ts src/types.ts --no-errors-on-unmatched</automated>
  </verify>
  <done>
    - package.json has bin field pointing to ./src/cli.ts with command name fb-ad-scraper
    - src/config.ts PresetSchema includes optional adLibraryUrl field with URL validation
    - src/cli.ts has --url flag in yargs options
    - config.example.json includes adLibraryUrl example with {query} placeholder
    - CliArgs type in src/index.ts includes url?: string
    - Biome reports no errors on modified files
  </done>
</task>

<task type="auto">
  <name>Task 2: Pipeline Wiring — Dynamic Ad Library URL</name>
  <files>src/scraper.ts, src/index.ts, src/types.ts</files>
  <action>
**Per D-07, D-08:** Wire URL precedence through the pipeline:

1. Update ScraperOptions in src/types.ts:
   - Add optional `url?: string` field (the resolved ad library URL with {query} already replaced)

2. Update src/index.ts main() function:
   - After loading config and resolving preset, determine the ad library URL using this precedence:
     a. CLI --url flag (highest priority, per D-07)
     b. Config preset adLibraryUrl field (per D-05)
     c. Default BASE_URL from scraper.ts (fallback, per D-08)
   - If URL is from config/CLI, replace `{query}` placeholder with `encodeURIComponent(query)` (per D-09)
   - Pass the resolved URL in ScraperOptions.url

3. Update src/scraper.ts runScraper():
   - Remove the hardcoded BASE_URL constant
   - Use `options.url` if provided, otherwise fall back to a default URL constant (the current BASE_URL value)
   - The URL passed in options.url should already have {query} replaced (done in index.ts)
   - Update the searchUrl construction: `const searchUrl = options.url ?? DEFAULT_BASE_URL`

**Important:** The default BASE_URL constant should remain in scraper.ts as a fallback (per D-08), but the primary path uses the URL from ScraperOptions.

Do NOT modify package.json, config files, or delete scraper.js in this task.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bunx biome check src/scraper.ts src/index.ts src/types.ts --no-errors-on-unmatched && grep -c 'options.url' src/scraper.ts && grep -c 'adLibraryUrl' src/index.ts</automated>
  </verify>
  <done>
    - ScraperOptions type includes url?: string field
    - src/index.ts resolves URL with precedence: CLI flag > config preset > default
    - {query} placeholder is replaced with URL-encoded query before passing to scraper
    - src/scraper.ts uses options.url when provided, falls back to DEFAULT_BASE_URL
    - No hardcoded BASE_URL used in primary path
    - Biome reports no errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Legacy Cleanup & Documentation</name>
  <files>scraper.js, README.md, .gitignore</files>
  <action>
**Per D-011:** Remove scraper.js from project root:
- Delete scraper.js file
- Keep example_page_code.html as reference (per D-011)

**Per D-012:** Update all references:
- Check .gitignore for any scraper.js references and remove them
- Check package.json scripts for any scraper.js references (currently none — start script already points to src/cli.ts)
- Verify no other files reference scraper.js

**Per D-013, D-014, D-015:** Create comprehensive README.md:
- Title: Facebook Ads Library Scraper
- Description: What the tool does (stealth scraper for Facebook Ads Library profile URLs)
- Installation: `bun install` or `npm install`
- Quick Start: `bun start "search query"` or `fb-ad-scraper "search query"` after global install
- Usage section: all CLI arguments with descriptions
- Configuration section: config.json format with complete example showing presets, adLibraryUrl, callback, proxy
- CLI Reference: table of all flags (--query, --preset, --url, --headless, --proxy, --max-urls, --max-no-new-scrolls, --daemon, --daemon-action, --callback, --env-file)
- Examples section: common usage patterns
- License: Apache 2.0 (matching existing LICENSE file)

The README should be complete enough that a new user can install, configure, and run the scraper without reading source code.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && test ! -f scraper.js && grep -c 'Installation' README.md && grep -c 'adLibraryUrl' README.md && grep -c 'fb-ad-scraper' README.md</automated>
  </verify>
  <done>
    - scraper.js is deleted from project root
    - example_page_code.html still exists
    - .gitignore has no scraper.js references
    - README.md contains: title, description, installation, quick start, usage, configuration, CLI reference, examples, license
    - README.md includes complete config.json example with adLibraryUrl field
    - README.md documents the --url flag and URL precedence behavior
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → Config | User-provided --url flag crosses into config resolution |
| Config file → Runtime | config.json loaded via cosmiconfig, parsed by Zod |
| npm install → bin | Global install creates fb-ad-scraper command |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-01 | Tampering | --url CLI flag | medium | mitigate | Zod URL validation on adLibraryUrl field; reject malformed URLs |
| T-01.1-02 | Information Disclosure | config.example.json | low | accept | Example file contains placeholder URLs only, no real credentials |
| T-01.1-03 | Elevation of Privilege | bin field | low | accept | bin field only registers local command; no system-wide escalation |
</threat_model>

<verification>
1. `bun start "test"` runs from project root without specifying src/cli.ts
2. `bunx tsx src/cli.ts --url "https://example.com/?q={query}" "test"` uses custom URL
3. Config preset with adLibraryUrl is used when --url not provided
4. Default BASE_URL is used when neither config nor CLI flag set
5. `scraper.js` no longer exists in project root
6. `bun test` passes (existing tests still work)
7. README.md is complete and accurate
</verification>

<success_criteria>
- User can run scraper from project root via `bun start "query"`
- User can install globally and run via `fb-ad-scraper "query"`
- User can set custom ad library URL per config preset
- User can override URL via --url CLI flag
- Precedence works: CLI > config > default
- Legacy scraper.js removed
- Comprehensive README.md created
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/01.1-make-a-entry-point-or-move-the-cli-to-the-root-so-i-don-t-ha/01.1-01-SUMMARY.md` when done
</output>
