---
phase: 07-7-daemon-actions-not-working-and-daemon-logs-not-being-saved
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/daemon-actions.ts
  - src/daemon.ts
  - src/cli.ts
  - package.json
  - .gitignore
  - test/unit/daemon.test.ts
  - test/integration/daemon.test.ts
  - test/unit/daemon-actions.test.ts
  - test/integration/daemon-logging.test.ts
autonomous: false
requirements: [DAEMON-01, DAEMON-02, DAEMON-03, DAEMON-04, DAEMON-05, TEST-01, TEST-02]
must_haves:
  truths:
    - "User can run bun daemon-actions stop to stop a running daemon"
    - "User can run bun daemon-actions status to check if daemon is running"
    - "User can run bun daemon-actions logs to view daemon log output"
    - "Daemon logs are written to .daemon.log file during execution"
    - "Tests do not leave artifacts in the project root"
    - ".gitignore includes .daemon.pid, .daemon.log, daemon.log, output/"
  artifacts:
    - path: "src/daemon-actions.ts"
      provides: "Standalone daemon management script"
      exports: ["stop", "status", "logs"]
    - path: "src/daemon.ts"
      provides: "Daemon with log file piping"
      contains: "pipe"
    - path: ".gitignore"
      provides: "Ignore daemon artifacts"
      contains: ".daemon"
    - path: "test/unit/daemon-actions.test.ts"
      provides: "Unit tests for daemon-actions script"
    - path: "test/integration/daemon-logging.test.ts"
      provides: "Integration tests for log file piping"
  key_links:
    - from: "src/daemon-actions.ts"
      to: "src/daemon.ts"
      via: "imports readPid, isProcessRunning, removePidFile"
      pattern: "import.*from.*daemon"
    - from: "src/daemon.ts"
      to: ".daemon.log"
      via: "child.stdout.pipe to log file"
      pattern: "createWriteStream.*daemon.log"
---

<objective>
Fix three daemon-mode bugs: daemon management commands (stop/status/logs) fail because yargs requires --query, daemon logs are not written to a file (pipes created but never connected), and tests leave artifacts in project root.

Purpose: Daemon mode is unusable without working management commands and log persistence. These are blocking issues for production use.
Output: Working daemon-actions script, log file piping, clean test isolation.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-7-daemon-actions-not-working-and-daemon-logs-not-being-saved/07-CONTEXT.md
@.planning/phases/07-7-daemon-actions-not-working-and-daemon-logs-not-being-saved/07-RESEARCH.md
@src/daemon.ts
@src/cli.ts
@src/logger.ts
@src/index.ts
@src/output.ts
@package.json
@.gitignore
@test/unit/daemon.test.ts
@test/integration/daemon.test.ts
@test/unit/daemon-actions.test.ts
@test/integration/daemon-logging.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create daemon-actions.ts and fix daemon log piping</name>
  <files>src/daemon-actions.ts, src/daemon.ts, src/cli.ts, package.json, .gitignore, test/unit/daemon-actions.test.ts, test/integration/daemon-logging.test.ts</files>
  <read_first>
    - src/daemon.ts (full file — PID operations, startDaemon, stopDaemon)
    - src/cli.ts (full file — daemon action handling at lines 126-153 to remove)
    - package.json (scripts section)
    - .gitignore (existing entries)
  </read_first>
  <behavior>
    - Test: Running `bun src/daemon-actions.ts status` with no daemon running prints "Daemon not running"
    - Test: Running `bun src/daemon-actions.ts stop` with no daemon running prints warning and exits cleanly
    - Test: Running `bun src/daemon-actions.ts logs` with no log file prints "No daemon log file found"
    - Test: Running `bun src/daemon-actions.ts` with no arguments prints usage and exits with code 1
    - Test: Running `bun src/daemon-actions.ts invalid` prints usage and exits with code 1
    - Test: PID_FILE constant equals '.daemon.pid'
    - Test: LOG_FILE constant equals '.daemon.log'
  </behavior>
  <action>
**Part A: Create src/daemon-actions.ts (per D-01 through D-06, D-07 through D-10, D-15)**

Create a new standalone script at `src/daemon-actions.ts` that handles daemon management without yargs. Key requirements:
- Simple `process.argv[2]` check for action (no yargs needed — per D-05)
- Three actions: `stop`, `status`, `logs` (per D-03)
- For `stop`: read PID via `readPid()`, check `isProcessRunning()`, send SIGTERM, wait 2s then verify PID file deleted, warn if still present (per D-07, D-08, D-09, D-10)
- For `status`: read PID, check running, print status (per D-03)
- For `logs`: read `.daemon.log` file, print contents, handle missing file (per D-15, uses `.daemon.log` not `daemon.log`)
- For unknown/missing action: print usage `Usage: bun daemon-actions <stop|status|logs>` and `process.exit(1)` (per D-06)
- Import `readPid`, `isProcessRunning`, `removePidFile` from `./daemon.js`
- Import `createLogger` from `./logger.js`
- Use `console.log` for status output, `console.error` for errors/usage
- The stop action should use `setTimeout` with 2000ms delay then check PID file existence — if PID file still exists, warn user but don't force-kill (per D-10)

**Part B: Fix daemon log piping in src/daemon.ts (per D-11 through D-14)**

Modify `startDaemon()` function in `src/daemon.ts`:
- Update `LOG_FILE` constant from `'daemon.log'` to `'.daemon.log'` (per D-11)
- After forking child process (line 78-82), add log stream piping:
  - Create `const logStream = fs.createWriteStream(LOG_FILE, { flags: 'a' })`
  - Pipe `child.stdout?.pipe(logStream)` and `child.stderr?.pipe(logStream)` (per D-12, D-13)
- The existing `fs.writeFileSync(LOG_FILE, '')` on line 75 already clears log on start (per D-14)

**Part C: Remove daemon-action handling from src/cli.ts (per D-04)**

Remove the `--daemon-action` option from yargs configuration (lines 45-49) and the entire daemon action handling block (lines 126-153). Also remove `daemonAction` from the `CliArgs` type in `src/index.ts` (line 25).

**Part D: Update package.json and .gitignore (per D-02, D-20)**

- Add npm script: `"daemon-actions": "bun run src/daemon-actions.ts"` (per D-02)
- Add to `.gitignore`: `.daemon.pid`, `daemon.log`, `.daemon.log`, `output/` (per D-20). Note: `output/` is already present in .gitignore (line 148) — verify it exists; `.daemon.pid`, `daemon.log`, and `.daemon.log` are new entries.

**Part E: Create test scaffolds for daemon-actions and log piping (Wave 0 gaps from VALIDATION.md)**

Create `test/unit/daemon-actions.test.ts` with the behavior tests from the `<behavior>` block above:
- Test `PID_FILE` constant equals `.daemon.pid`
- Test `LOG_FILE` constant equals `.daemon.log`
- Test running with no arguments prints usage and exits with code 1
- Test running with invalid action prints usage and exits with code 1
- Test status with no daemon running prints "Daemon not running"
- Test stop with no daemon running prints warning and exits cleanly
- Test logs with no log file prints "No daemon log file found"
- Use `/tmp/` temp directory for any file I/O (per D-16, D-18)

Create `test/integration/daemon-logging.test.ts` to verify log file piping:
- Test that starting a daemon creates `.daemon.log` file
- Test that daemon stdout/stderr output appears in `.daemon.log`
- Use `/tmp/` temp directory for test artifacts (per D-16, D-18)
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test test/unit/daemon.test.ts 2>&1; echo EXIT:$?</automated>
  </verify>
  <done>
    - src/daemon-actions.ts exists and exports handleStop, handleStatus, handleLogs functions
    - LOG_FILE constant in daemon.ts equals '.daemon.log'
    - startDaemon() pipes child.stdout and child.stderr to WriteStream
    - --daemon-action option removed from cli.ts yargs config
    - daemonAction removed from CliArgs type in index.ts
    - package.json has "daemon-actions" script
    - .gitignore includes .daemon.pid, .daemon.log, daemon.log, output/ (per D-20)
    - test/unit/daemon-actions.test.ts exists with behavior tests from <behavior> block
    - test/integration/daemon-logging.test.ts exists with log piping verification tests
    - bun test test/unit/daemon.test.ts passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor daemon PID/log functions for test isolation — use /tmp/facebook-scraper-test/ for all test file I/O</name>
  <files>src/daemon.ts, test/unit/daemon.test.ts, test/integration/daemon.test.ts</files>
  <read_first>
    - src/daemon.ts (full file — writePid, readPid, removePidFile functions and PID_FILE, LOG_FILE constants)
    - test/unit/daemon.test.ts (full file — imports PID_FILE, LOG_FILE, testDir usage)
    - test/integration/daemon.test.ts (full file — imports PID_FILE, LOG_FILE, testDir usage)
  </read_first>
  <action>
**Part A: Refactor daemon.ts PID/log functions to accept optional path parameter (per D-18, D-19)**

Modify `src/daemon.ts` so `writePid`, `readPid`, `removePidFile`, and any file I/O helper accepts an optional `filePath` parameter:
- `writePid(pid: number, filePath?: string)` — if provided, write to `filePath` instead of `PID_FILE`
- `readPid(filePath?: string)` — if provided, read from `filePath` instead of `PID_FILE`
- `removePidFile(filePath?: string)` — if provided, remove from `filePath` instead of `PID_FILE`
- Keep the default behavior (no param) using `PID_FILE` constant for production use
- The `isProcessRunning` function does NOT need a path parameter (it takes a pid number)

**Part B: Update test files to use shared temp directory and pass custom paths (per D-16 through D-19)**

Modify `test/unit/daemon.test.ts`:
- Import `join` from `path`
- Replace hardcoded test directory with shared: `const testDir = '/tmp/facebook-scraper-test'` (per D-16 — shared temp dir for all test file I/O)
- Create testDir in `beforeEach`: `await fs.promises.mkdir(testDir, { recursive: true })`
- Define `PID_FILE_PATH = join(testDir, PID_FILE)` and `LOG_FILE_PATH = join(testDir, LOG_FILE)`
- Call `writePid(pid, PID_FILE_PATH)`, `readPid(PID_FILE_PATH)`, `removePidFile(PID_FILE_PATH)` — pass the temp path so tests never touch CWD (per D-18)
- Update the `PID file constants` test: `LOG_FILE` should now be `'.daemon.log'` (not `'daemon.log'`)
- Ensure `afterEach` removes the testDir (all PID/LOG files live inside it, so cleanup is clean — per D-17)

Modify `test/integration/daemon.test.ts`:
- Same shared testDir pattern: `const testDir = '/tmp/facebook-scraper-test'` (per D-16)
- Same `PID_FILE_PATH` / `LOG_FILE_PATH` approach with `join(testDir, PID_FILE)`
- Update `LOG_FILE` constant test from `'daemon.log'` to `'.daemon.log'`
- Ensure `afterEach` removes testDir

**Key principle:** After every test run, zero files should exist in the project root. All PID/LOG operations go to `/tmp/facebook-scraper-test/` via the optional path parameter.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test test/unit/daemon.test.ts test/integration/daemon.test.ts 2>&1; echo EXIT:$?</automated>
  </verify>
  <done>
    - src/daemon.ts: writePid, readPid, removePidFile accept optional filePath parameter
    - test/unit/daemon.test.ts: LOG_FILE assertion updated to '.daemon.log', all PID/LOG writes use temp dir via path parameter
    - test/integration/daemon.test.ts: LOG_FILE assertion updated to '.daemon.log', all PID/LOG writes use temp dir via path parameter
    - No .daemon.pid or .daemon.log files remain in project root after tests
    - bun test test/unit/daemon.test.ts test/integration/daemon.test.ts passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify daemon management commands work end-to-end</name>
  <files>src/daemon-actions.ts, src/daemon.ts, src/cli.ts, package.json, .gitignore, test/unit/daemon.test.ts, test/integration/daemon.test.ts, test/unit/daemon-actions.test.ts, test/integration/daemon-logging.test.ts</files>
  <action>
Verify all daemon management fixes work correctly:

1. Run `bun run daemon-actions status` — should print "Daemon not running" (no yargs query error)
2. Run `bun run daemon-actions stop` — should print warning about no daemon, not crash
3. Run `bun run daemon-actions logs` — should print "No daemon log file found" (not crash)
4. Run `bun run daemon-actions` — should print usage help and exit with code 1
5. Run `bun test` — all tests should pass with no .daemon.pid or .daemon.log files left in project root
6. Verify `src/cli.ts` no longer has `--daemon-action` option
7. Verify `.gitignore` includes .daemon.pid and .daemon.log

Built artifacts:
- src/daemon-actions.ts: standalone daemon management script with stop/status/logs actions
- src/daemon.ts: log file piping (child.stdout/stderr → .daemon.log)
- src/cli.ts: removed --daemon-action flag
- package.json: added "daemon-actions" npm script
- .gitignore: added .daemon.pid, .daemon.log
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun run daemon-actions status 2>&1 | grep -q 'Daemon not running' && echo "status OK" && bun run daemon-actions stop 2>&1 | grep -q 'Daemon not running' && echo "stop OK" && bun run daemon-actions logs 2>&1 | grep -q 'No daemon log file found' && echo "logs OK" && bun run daemon-actions 2>&1; test $? -eq 1 && echo "usage OK" && bun test 2>&1; echo EXIT:$?</automated>
  </verify>
  <done>
    - `bun run daemon-actions status` works without yargs query error
    - `bun run daemon-actions stop` works without yargs query error
    - `bun run daemon-actions logs` works without yargs query error
    - All tests pass
    - No artifacts left in project root after tests
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → daemon-actions.ts | User-provided action string parsed from process.argv |
| daemon-actions.ts → daemon.ts | PID file read/write, signal sending |
| startDaemon → child process | Fork with env marker, stdio piping |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | PID file race condition | medium | mitigate | proper-lockfile already implemented in acquirePidLock() |
| T-07-02 | Denial of Service | Stale PID file | low | mitigate | isProcessRunning check before sending signals in daemon-actions.ts |
| T-07-03 | Tampering | Log file injection | low | accept | pino logger with structured output; log file is for debugging only |
</threat_model>

<verification>
- `bun test test/unit/daemon.test.ts` passes
- `bun test test/integration/daemon.test.ts` passes
- `bun test test/unit/daemon-actions.test.ts` passes (new — Wave 0 gap filled)
- `bun test test/integration/daemon-logging.test.ts` passes (new — Wave 0 gap filled)
- `bun test` — full suite passes with no project root artifacts
- `bun run daemon-actions status` runs without yargs query error, prints "Daemon not running"
- `bun run daemon-actions stop` runs without yargs query error, prints warning about no daemon
- `bun run daemon-actions logs` runs without yargs query error, prints "No daemon log file found"
- `bun run daemon-actions` with no args prints usage and exits with code 1
- No `.daemon.pid` or `.daemon.log` files remain in project root after test run
</verification>

<success_criteria>
- Daemon management commands (stop/status/logs) work via `bun run daemon-actions <action>`
- Daemon logs are persisted to `.daemon.log` file during execution
- Tests do not leave artifacts in project root
- All existing tests pass
- New test files exist: test/unit/daemon-actions.test.ts, test/integration/daemon-logging.test.ts
- .gitignore includes .daemon.pid, .daemon.log, daemon.log, output/ (per D-20)
- No new dependencies added
</success_criteria>

<output>
Create `.planning/phases/07-7-daemon-actions-not-working-and-daemon-logs-not-being-saved/07-01-SUMMARY.md` when done
</output>
