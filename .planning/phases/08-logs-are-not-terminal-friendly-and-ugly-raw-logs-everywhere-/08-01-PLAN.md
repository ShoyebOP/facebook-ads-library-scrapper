---
phase: 08-logs-are-not-terminal-friendly-and-ugly-raw-logs-everywhere-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger.ts
  - src/index.ts
autonomous: true
requirements:
  - ERROR-01
  - ERROR-05
must_haves:
  truths:
    - CLI log output shows formatted text like "INFO [scraper]: Navigating to Ads Library..." instead of raw JSON
    - Daemon log file contains formatted text without ANSI color codes
    - Scrape completion message shows elapsed time: "Scrape complete: N URLs in Xm Ys"
    - Proxy credentials remain redacted in all log output (no plaintext passwords)
    - Log levels display as abbreviated uppercase: INFO, WARN, ERROR, DEBUG
    - Module names appear as bracketed prefix: [scraper], [scroll], [webhook]
    - No timestamps in CLI or daemon log output
  artifacts:
    - src/logger.ts (modified with pino-pretty stream integration)
    - src/index.ts (modified with elapsed time tracking)
    - node_modules/pino-pretty (installed dependency)
  key_links:
    - createLogger detects CLI vs daemon via SCRAPER_LOG_FILE env var
    - pino-pretty stream API wraps pino (not transport — Bun incompatible)
    - child logger {module} property feeds into messageFormat template
---

<objective>
Transform raw pino JSON log output into polished, human-readable formatted text using pino-pretty stream API.

Purpose: Make logs readable in both interactive CLI sessions and daemon log files, improving developer experience and operational visibility.
Output: Modified `src/logger.ts` with pino-pretty integration, modified `src/index.ts` with elapsed time completion message, installed `pino-pretty` dependency.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-logs-are-not-terminal-friendly-and-ugly-raw-logs-everywhere-/08-CONTEXT.md
@.planning/phases/08-logs-are-not-terminal-friendly-and-ugly-raw-logs-everywhere-/08-RESEARCH.md
@.planning/phases/08-logs-are-not-terminal-friendly-and-ugly-raw-logs-everywhere-/08-PATTERNS.md
@src/logger.ts
@src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install pino-pretty and rewrite logger.ts with stream API</name>
  <files>src/logger.ts, src/logger.test.ts</files>
  <read_first>
    - src/logger.ts — current createLogger/createChildLogger implementation
    - node_modules/pino/README.md — pino stream API signature
    - node_modules/pino-pretty/README.md — PrettyOptions interface, messageFormat syntax
  </read_first>
  <behavior>
    - Test: createLogger() in CLI mode (no SCRAPER_LOG_FILE) returns a pino logger that outputs formatted text to stdout, not raw JSON
    - Test: createLogger() in daemon mode (SCRAPER_LOG_FILE set) returns a pino logger that writes formatted text to the specified file
    - Test: formatted output contains level label (INFO/WARN/ERROR/DEBUG) before module name
    - Test: formatted output contains module name in brackets: [scraper], [scroll], etc.
    - Test: formatted output does NOT contain timestamps
    - Test: formatted output does NOT contain pid or hostname
    - Test: proxy credentials are redacted in output (password replaced with [Redacted])
    - Test: daemon log file contains no ANSI escape codes (no \x1b[ or \e[ sequences)
    - Test: createChildLogger produces logger with {module} property accessible in output
  </behavior>
  <action>
1. Run `bun add pino-pretty` to install the dependency (per D-01, package audit: pino-pretty is OK — 7+ years, 16.7M weekly downloads).

2. Rewrite `src/logger.ts` to use pino-pretty as a stream (NOT transport — Bun worker_threads are incompatible, per research pitfall 1):

   - Add `import pinoPretty from 'pino-pretty'` alongside existing `import pino from 'pino'`
   - Keep `import fs from 'fs'` (needed for daemon file stream)
   - Keep existing `redact: ['proxy', '*.proxy']` config (ERROR-05 proxy credential sanitization)

3. Rewrite `createLogger()` function:

   - Check `process.env.SCRAPER_LOG_FILE` to detect daemon mode (existing pattern)
   - **CLI mode** (no env var): Use `pino({ level, redact }, pinoPretty({ ... }))` with these options per locked decisions:
     - `colorize: true` (D-01: terminal output)
     - `levelFirst: true` (D-02: level before module)
     - `ignore: 'pid,hostname'` (D-03: no timestamps, no pid/hostname)
     - `translateTime: false` (D-03: no timestamps)
     - `messageFormat: '{levelLabel} [{module}]: {msg}'` (D-04: bracketed module prefix)
   - **Daemon mode** (env var set): Use same pino-pretty config but:
     - `colorize: false` (D-06: no ANSI codes in log file)
     - `destination: logFile` (write to file, not stdout)
     - `append: true, mkdir: true` (append to existing log)
     - Same `levelFirst`, `ignore`, `translateTime`, `messageFormat` (D-05: same format)

4. Keep `createChildLogger()` unchanged — child logger `{module}` property feeds into `messageFormat` template.

5. Add tests in `src/logger.test.ts`:
   - Test CLI mode outputs formatted text (capture stdout, verify contains "INFO" and "[")
   - Test daemon mode writes formatted text to temp file (set SCRAPER_LOG_FILE, verify file content)
   - Test no timestamps in output
   - Test no ANSI codes in daemon log
   - Test proxy redaction preserved
   - Test child logger module name appears in output

6. Commit: `feat(08-01): add pino-pretty stream formatting for CLI and daemon logs`

   **CRITICAL:** Do NOT use `pino({ transport: { target: 'pino-pretty' } })` — this crashes in Bun. Always use stream API: `pino(options, pinoPretty(streamOptions))`.
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun test src/logger.test.ts</automated>
  </verify>
  <done>
    - `bun add pino-pretty` completes without errors
    - `src/logger.ts` exports `createLogger()` that returns pino logger with pino-pretty stream
    - CLI mode: stdout receives formatted text like "INFO [scraper]: message"
    - Daemon mode: log file receives formatted text without ANSI codes
    - Proxy credentials redacted in all output
    - All tests in `src/logger.test.ts` pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update index.ts completion message with elapsed time</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts — current main() function, completion message at line 165-167
  </read_first>
  <behavior>
    - Test: completion message matches format "Scrape complete: N URLs in Xm Ys"
    - Test: elapsed time is accurate (within 1 second tolerance)
    - Test: URL count is correct
  </behavior>
  <action>
1. In `src/index.ts`, add elapsed time tracking around `runScraper()` call:

   - Record `const startTime = Date.now()` before `runScraper(options)` (around line 162)
   - After `runScraper` returns, calculate elapsed time:
     ```
     const elapsedMs = Date.now() - startTime;
     const elapsedMin = Math.floor(elapsedMs / 60000);
     const elapsedSec = Math.floor((elapsedMs % 60000) / 1000);
     ```

2. Update completion message per D-09:

   - Replace current message `"Scraping complete: ${urls.size} unique profile URLs collected"` with:
     `"Scrape complete: ${urls.size} URLs in ${elapsedMin}m ${elapsedSec}s"`
   - This matches D-09 format: `'Scrape complete: 142 URLs in 3m 22s'`

3. Keep all other logging unchanged (D-08: keep existing log messages and heartbeat format).

4. Add a simple test (optional — can be inline verification):
   - Run CLI with a query and verify completion message contains "Scrape complete:" and "URLs in"

5. Commit: `feat(08-01): add elapsed time to scrape completion message`
  </action>
  <verify>
    <automated>cd /home/shoyeb/projects/facebook-ads-library-scrapper && bun run tsc --noEmit</automated>
  </verify>
  <done>
    - Completion message shows "Scrape complete: N URLs in Xm Ys"
    - Elapsed time is calculated from runScraper start to completion
    - TypeScript compiles without errors
    - All existing tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Logger | Log messages may contain user-controlled data (query strings, URLs) |
| Proxy config → Logs | Proxy credentials must never appear in log output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | src/logger.ts | high | mitigate | Proxy credentials redacted via pino `redact: ['proxy', '*.proxy']` — pino-pretty respects redaction config |
| T-08-02 | Tampering | pino-pretty stream | low | accept | pino-pretty sanitizes output; no raw user input reaches log formatting layer |
| T-08-03 | Information Disclosure | daemon log file | medium | mitigate | colorize: false prevents ANSI injection; file permissions inherit from process umask |
</threat_model>

<verification>
- Run `bun run src/cli.ts "test" --headless` and observe formatted output in terminal
- Run `bun run src/cli.ts "test" --daemon`, then `bun run src/daemon-actions.ts logs` and verify clean formatted text without ANSI codes
- Verify completion message shows "Scrape complete: N URLs in Xm Ys"
- Run `bun test` to confirm all tests pass
</verification>

<success_criteria>
- CLI terminal shows formatted logs: "INFO [scraper]: Navigating to Ads Library..."
- Daemon log file contains same format without ANSI escape codes
- Completion message: "Scrape complete: 142 URLs in 3m 22s"
- Proxy credentials never appear in any log output
- All existing tests pass
- No pino.transport() usage (would crash in Bun)
</success_criteria>

<output>
Create `.planning/phases/08-logs-are-not-terminal-friendly-and-ugly-raw-logs-everywhere-/08-01-SUMMARY.md` when done
</output>
